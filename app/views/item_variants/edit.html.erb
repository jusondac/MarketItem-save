<div class="container pt-5">
  <div class="card" style="width: 30rem;">
    <div class="card-body">
      <h5 class="card-title bg-primary text-white">Editing Jenis Item</h5>
      <%= render 'form', item_variant: @item_variant %>
      <div class="row mt-3">
        <div class="col">
          <%= link_to 'Show', @item_variant, class:'btn btn-primary' %>
        </div>
        <div class="col">
          <%= link_to 'Back', item_variants_path, class:'btn btn-secondary' %>
        </div>
      </div>
    </div>
  </div>
</div>
